const OperationType = Object.freeze({
  'grab gold nugget': 'grab gold nugget',
  'build nest': 'build nest',
});

const Role = Object.freeze({
  'cow-bow': 'cow-boy',
  chicken: 'chicken',
  goldingue: 'goldingue',
  nest: 'nest',
});

export { OperationType, Role };
